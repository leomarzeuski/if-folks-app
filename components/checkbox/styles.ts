import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
    label: {
        marginLeft: 16,
        fontSize: 16,
      },
      checkbox: {
        borderColor: "#ACB1C6",
        borderRadius: 4,
        fontSize: 12,
      },
    });